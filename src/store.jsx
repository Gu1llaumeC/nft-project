// import { createStore } from 'redux'

// function account(state = [], action) {
//   switch (action.type) {
//     case 'SET_ACCOUNT':
//       return state.concat([action.text])
//     default:
//       return state
//   }
// }

// export const store = createStore(account, ['Use Redux'])